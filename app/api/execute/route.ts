import { NextResponse } from 'next/server';

export async function POST(req: Request) {
  // Placeholder for Judge0 integration (Stage 3)
  return NextResponse.json({ message: 'Execution endpoint' });
}